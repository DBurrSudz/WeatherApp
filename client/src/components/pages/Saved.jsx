import React from "react";
import { Typography } from "@material-ui/core";

const SavedWeather = () => {
    return (
        <div>
            <Typography variant="h2">Saved Weather</Typography>
        </div>
    );
};

export default SavedWeather;
